# AI 小说生成系统　提示词与前置创作流程规范（Prompting & Worldbuilding Spec）

> 版本：v1.0　|　日期：2026-09-20
> 关联：设计 `AI小说生成系统-设计文档.md`（§5.1 初始化、§8 去AI味、StyleProfile）、需求 `AI小说生成系统-需求文档.md`（FR-1/FR-3/FR-4/FR-7）
> 目的：明确"提示词从哪来、如何组织、世界观/历史/大纲如何前置实现"，做到可追溯、可执行、合规。

---

## 1. 提示词借鉴来源（合规优先级）

> ⚠️ 许可提醒：**AGPL / GPL 项目**整段复制其 prompt 会把你项目的派生作品也归入该许可（传染）；**MIT/免费**项目可安全复制（MIT 需保留版权声明）。提示词作为软件产物同样受此约束。

| 借鉴对象 | 许可 | 借鉴内容 | 方式 |
|----------|------|----------|------|
| **oh-story-claudecode** | MIT | 中文网文**全流程方法论**：先搭纲再写正文、伏笔/双时间线追踪、`deslop` 去AI味规则、扫榜/拆文思路 | 主抄：结构 + 方法论文档，改造成本系统 prompt |
| **storyforge** | MIT | `writing-styles.ts`（文风预设表）、`anti-ai-adapter.ts`（去AI味适配器）、`prompt-engine`（{{var}}/{{#if}} 模板）、任务路由 seed | 主抄：文风与去AI味的最结构化模板 |
| **chinese-novelist-skill** | 免费 | 三层问答（L1定位/L2定制）、7 列大纲、章末摘要、"结尾截断不总结"黄金法则 | 借鉴流程思路 |
| **NovelClaw** | MIT | world_agent / character_agent / plot_agent 的角色分工 prompt | 可借鉴 Agent 分工提示词 |
| **AI-Novel-Writing-Assistant** | ⚠️ AGPL | "自动导演分层开书"（书级→卷→节奏板→章节清单）、写法引擎+反AI规则 | **只借鉴架构思想，不复制文案** |
| **neuro-book** | ⚠️ AGPL | llmlint 360 条去AI味规则的**分类思想**（填充词/机械过渡/公式化设问…） | **只借鉴分类，规则文案自写** |

**本项目主抄组合**：`storyforge`（文风/去AI味模板）+ `oh-story-claudecode`（网文全流程方法论），前端用 Vue 复刻 storyforge 的"文风预设表"数据结构即可。所有 prompt 统一维护在 `contracts/prompts/`（见 §5），避免散落。

---

## 2. 风格 / 文风（StyleProfile）

复用 storyforge `writing-styles.ts` 的模式：**文风 = 一组可注入 system prompt 的结构化约束 + 温度**。

```jsonc
// StyleProfile 预设（存库，字符串引用 system_prompt 模板）
{
  "id": "wu-xiu",                    // 名称
  "style_name": "金庸武侠式",
  "temperature": 0.75,
  "system_prompt_ref": "style/wuxia",
  "theme":    "侠义与代价",          // 主题：故事在探讨什么（救赎/自由/科技伦理…）
  "tone":     "史诗感 / 和缓克制",    // 基调：热血/压抑/诙谐/史诗感，决定语言与走向
  "vocabulary":   { "倾向词汇": ["江湖","侠义","青衫"], "禁用词": ["简直","居然"] },
  "dialogue_style": "克制、少心理独白、以动作/神态代言",
  "narrative_distance": "全知第三人称 / 近距离跟随主角",   // 叙事视角（呼应方法论"选视角"）
  "anti_ai_rules": "refer to §4",
  "avoid_meta": ["不值得提的是","总而言之","让我们看看"]
}
```
> `theme` / `tone` 同步进入前置流程（见 §3.1 ①），并写入 `Story`，保证全篇基调统一。
**提供 ≥5 个预设**（现代都市、玄幻、武侠、悬疑、克苏鲁等），且允许用户**自定义**（追加/覆盖词汇与禁用词）。

---

## 3. 前置创作流程：世界观 / 历史 / 大纲 怎么一步步实现

> 对应需求 FR-1、设计 §5.1。核心原则：**"三段前置"必须产出结构化数据，未就绪不进入正文写作**（门禁参考 oh-story 的 guard-outline-before-prose）。

### 3.1 阶段总览（每次生成前由 Orchestrator 强制校验）

```
灵感一句话
 └─① 提炼核心概念 premise + 确定主题与基调：
        premise = 主角 + 目标 + 障碍 + 代价（例：失忆杀手想找回身份，但线索指向自己是叛徒，
               若真相揭开将失去爱人）；theme(主题) + tone(基调) 写入 Story/StyleProfile
     │  再生成 3 套「整本方向」候选（标题+一句话主线+题材基调），用户选1或重roll
      └─② 世界观设定（Worldbuilding）
          世界规则/地图/力量体系(魔法·科技·等级)/政治格局/组织势力/代价与限制
          └─③ 历史线 + 当前时间线
              历史线：过去的重大事件 → 势力的由来 → 社会现状的成因
              当前线：故事"现在"的 day/tick（随时间推进回写）
               └─④ 角色设定（主角/配角 + 关系网 + 人物弧光 + 初始状态）
                    └─⑤ 大纲：卷战略 → 章节细纲
                        每个细纲含：本章目标/冲突/转折/伏笔埋设点/卷内去向
                          └─⑥ 节奏板（可选）：全书情绪/进度曲线
                               └─ 【门禁】细纲未 100% 就绪 → 禁止 WriterAgent 写正文
```

### 3.2 每一阶段的实现方式（对应 Agent 与数据）

| 阶段 | 执行 Agent | 产出 JSON/文档 | 落库位置 | 是否用户参与 |
|------|-----------|----------------|----------|--------------|
| ① 整本方向 | DirectionGenerator | 3 套方向候选 + 题材基调 | `Story.synopsis` / `genre` | ✅ 选1或重roll |
| ② 世界观 | DirectionGenerator / world 子 Agent | 世界规则/地图/力量体系/势力 | `WorldState.state` | 可选补充 |
| ③ 历史线 | 历史推演 Agent | 历史大事表 + 成因链 | `TimelineEvent`（历史段） | 可选补充 |
| ④ 角色 | character 子 Agent | 角色卡 + 关系 + 弧光 | `Character` 表 | ✅ 可确认 |

> **角色设计要点（注入 character 子 Agent 提示词，源自通用写作方法论）**：
> 主角需**外在目标 ≠ 内在需求**（两者常冲突，构成成长弧光）+ 缺点/软肋；反派**不是纯恶**，要有自身逻辑与动机；每个配角应**推动情节或映衬主角**；角色卡含姓名/外貌/性格/背景/口头禅/秘密。
> 落库：`Character` 增加 `outer_goal`、`inner_need`、`flaw`、`lie`(认知误区)、`arc`(弧光阶段)。
| ⑤ 大纲 | planner 子 Agent | 卷战略 + 章节细纲（含伏笔点） | `Outline` 表 | ✅ 可确认 |
| ⑥ 节奏板 | 可选 | 情绪/进度曲线 | `Outline.node_type` | 可选 |

### 3.3 关键门禁（写到 Orchestrator 里，硬校验）
- **G1**：`Outline` 中当前卷/章节细纲未就绪 → WriterAgent 拒绝启动（返回 `CONFLICT` 式提示）。
- **G2**：写入正文前必须存在 `Character`（无主角）→ 拒绝。
- **G3**：世界观含"力量体系/限制"缺失时，仅允许**生活化/日常**段落后置补齐（降级为提示，不硬拦）。

### 3.4 "历史"为什么单独拆出来（不是只给模型一段文字）
- 历史线写成 `TimelineEvent`，由 ConsistencyChecker 复用做**时间线一致性校验**（防止"亡国三年后的男主忘了国亡"这类吃书）。
- 世界观/历史段本身也进 L2 RAG，保证长文中后期按需召回，而不是塞满每次 prompt。

### 3.5 可选故事结构模板（planner 子 Agent 的"骨架预设"）
> 对应通用方法论第 6 步。planner 依据题材/用户偏好选择一种模板，把它翻译成 `Outline`（卷→章）的分段依据。默认三幕剧；短篇/互动可换用其他。

| 结构 | 适用 | 落到 Outline 的方式 |
|------|------|---------------------|
| **三幕剧**（默认，约 25% / 50% / 25%） | 完整长篇小说 | 一幕=一个卷段；标注 激励事件 / 中点转折 / 黑暗时刻 / 顶点 / 新平衡 作为关键 `node_type` |
| **起承转合** | 短篇、单元剧 | 四段式节奏，配章节标题 |
| **英雄之旅**（12 步） | 冒险/成长 | 映射到卷与关键节点（启程/试炼/回归） |
| **雪花法** | 从一句逐步扩写 | 一句话→一段→一页→全书，用于自动扩展细纲 |

> 节奏板（情绪/进度曲线）进一步约束它的张力分布；多结构模板与抽卡机制兼容——`decision_point` 落在关键节点处更有效。

---

## 4. 去 AI 味规则（借鉴分类，规则自写）

按 **neuro-book llmlint 的分类思想 + storyforge anti-ai 的落地方式** 自建规则库（避免 AGPL 文案传染）：

| 规则类 | 示例（命中模式） | 分级 | 修复建议 |
|--------|------------------|------|----------|
| 填充词/口头禅 | 「居然」「简直」「值得一提」 | 阻断级 | 删/替换 |
| 机械过渡 | 「然而」「与此同时」「转眼间」高频连用 | 建议级 | 改写为动作承接 |
| 公式化设问 | 「你知道吗？（反问排比）」 | 阻断级 | 去反问落回叙事 |
| 二元对比/空泛总结 | 「他既…又…」「总之，一切似乎……」 | 建议级 | 具体化、落一个细节 |
| 呆板句式重复 | 「他X了。他Y了。」同句头连续 | 建议级 | 句法变奏 |
| 过度内心解说 | 解释动机过长、情感标签化 | 建议级 | 用动作/对白代替 |

**执行方式**：静态规则秒级全稿扫描（脚本级）+ 少量 LLM 语境复核（避免误伤），输出「定位+原因+替换建议」，交给 FixerAgent 或用户确认。

### 4.1 写作方法论：展示而非告知（构造路径，与去 AI 味正向互补）

> §4 去 AI 味是**剔除路径**（哪句不对 → 报错改掉），但它只告诉模型"这句不行"，给不出"该往哪个方向写"。本节是**构造路径**：正文前的用词/结构导向，二者正交——去 AI 味管"别写哪样"，方法论管"该写成哪样"。

**与文风（§2）正交**：文风决定腔调（金庸的凝练 vs 都市的冷峻），方法论决定"展示还是告知"，可安全叠加。**与伏笔/叙事状态（§3.4、narrative）分界**：方法论只对本段**局部真实**负责，跨段铺垫与叙事走向由前文设定/伏笔账本控制，不在单段里硬塞。

在正文 Writer 的 system 中追加注入（`writer.py` 的 `_system`，紧接 `[文风要求]` 之后）：

```
[写作方法论：展示而非告知]
本段只对自己负责（局部真实），跨段铺垫与叙事走向由前文设定控制，不在本段硬塞。
- 1. 场景是感觉得到的，不是说得出的：每个关键场景至少从声音、尺度/数字、画面、人物动作、生存逻辑中取两样落地；不直呼"恐怖/神秘/重要"，让读者自己去感受。
- 2. 人物活在动作与对话里，不用标签：用具体动作、语气、生活细节刻画性格；不用"目光锐利如夜行猫""微微一笑""目光深邃"这类套话；旧伤、家变、宿怨等关键过往须先铺垫，禁止突然空降。
- 3. 因果要付代价，线索不白给：帮助与信物须有动机、隐瞒或代价，不能无缘白送；"此物藏有奥秘"须用异象/异感证明，不能只靠台词交代；重大抉择要带出追兵、风险、牺牲等真实阻力。
- 4. 语言有节奏：长短句交错，动词准确；禁堆副词/形容词，忌文白夹杂。避免填充词：仿佛、似乎、缓缓、微微、一丝、闪过。
- 5. 一致性：人名、地名、设定前后统一，杜绝混用。
```

**落地**：作为 writer 的 system 层随 `文风要求` 一并注入（改动在 `backend/app/services/writer.py`）；其中第 4 条的填充词已并入去 AI 味密度规则（`deslop/lint.py` 的 `showcase-filler`，出现 ≥3 次报建议级）。**不改 `compare` 文风对比链路**——那要隔离"文风"单一变量，混入方法论会让对比失真。

---

## 5. 提示词工程落地（可复现、可追溯）

- **集中管理**：所有 prompt 模板存 `backend/app/prompts/`（按任务分目录：direction/plan/character/world/writer/consistency/deslop/summarize…），不在代码里散落字符串。
- **版本化**：每个 prompt 带 `version` 与 `changelog`；模型/提示词变更走"回归基线"（见开发计划 §4.3 LLM 评估集），防止悄悄退化。
- **按任务路由**：LLM Gateway 按任务选模型+温度。参考：
  - 前置设定（世界观/历史/大纲）→ 更稳定：temperature 0.3~0.5，可长思考模型。
  - 正文 Writer → 更具文采：temperature 0.7~0.85。
  - 一致性/审稿/去AI味 → 最稳：temperature 0.0~0.2。
- **抽卡方向生成**：DirectionGenerator 的卡池 prompt 要求**强 schema 输出**（`DirectionSpec`），与当前状态强相关，SSR 强制 `risk_balance`；多卡生成用冷却/质量过滤。

---

## 6. 抽卡与前置构建的融合（本设计亮点）
前置阶段（世界观/历史/大纲）与核心抽卡机制打通：
- **世界观扩展卡**：分歧点可选"补一条世界观设定"作为 EVENT 卡方向。
- **历史事件卡**：可把历史线中的重大事件作为"被触发/揭晓"，推动当前剧情。
- **伏笔卡**：与 `Foreshadow` 表联动，埋下即记账，后续抽到/触发时自动"兑付"并更新伏笔状态。

这样"前置构建"不只是冷启动的门槛，而是持续成为抽卡决策的素材来源，让世界观/历史始终参与剧情演进。