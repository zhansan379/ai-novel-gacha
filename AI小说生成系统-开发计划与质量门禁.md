# AI 小说生成系统　开发计划与质量门禁（Development Plan & Quality Gate）

> 版本：v1.0　|　日期：2026-09-20
> 关联：需求 `AI 小说生成系统-需求文档.md`、设计 `AI 小说生成系统-设计文档.md`、契约 `AI 小说生成系统-接口契约.md`

---

## 1. 里程碑与交付节奏

假设规模：**小团队（2 后端 + 1 前端 + 1 兼职 QA/PM），Go/UX 简化，MVP 目标 6 周（不含架构评审）**。每个里程碑都有明确"出口标准"，满足才能关闭。

| 里程碑 | 周期 | 交付物 | 出口标准（Gate） |
|--------|------|--------|------------------|
| M0 基线冻结 | D1–D3 | 需求评审 + 接口契约 v1 + 数据模型 + 技术栈锁定 | 四份文档 review 通过；接口字段全量签批；**无 P0 待澄清项** |
| M1 数据与骨架 | D4–D8 | LLM Gateway、StateStore（SQLite）、基本 REST（项目/故事 CRUD） | 健康检查通过；CRUD 冒烟用例通过；错误码符合契约 |
| M2 正文生成 | D9–D16 | WriterAgent + 流式 SSE + 章节存储 | 端到端：一段正文 SS流式落地；TTFB≤3s；可中断/恢复 |
| M3 决策与抽卡 | D17–D25 | 分歧点检测、卡池生成、盲抽/明选、自由输入意图路由、DecisionLog/Branch | **核心玩法闭环可用**：抽一卡→方向→生成→再分歧（验收见 §3） |
| M4 一致性与质量 | D26–D33 | ConsistencyChecker、伏笔账本、去AI味 lint、审稿-修复、时间线 | 30 章一致性回归；去AI味扫描达标；审稿端到端 |
| M5 前端界面 | D20–D40（与 M3/M4 并行） | 决策页（抽卡动画/明选/输入框）、正文流式页、设定/图鉴/历史分支视图、项目列表 | 核心路径 UI 走查通过；决策≤3次点击；主流浏览器无错 |
| M6 收尾 | D41–D48 | BYOK 多模型、token 看板、权限、UAT | 内测 UAT 通过；假设 §4 质量基线全绿；发布评审 |

> D 为工作日。M3 与前端部分存在依赖（前端需等 M3 出决策 API），故 M3 放行后前端急起。

---

## 2. 任务拆分与负责人（RACI）

| 任务 | 负责人（Owner） | 依赖 | 对应需求 |
|------|----------------|------|----------|
| LLM Gateway + BYOK + token 预算 | 后端A | 架构冻结 | FR-10 |
| StateStore + 数据模型 + 迁移 | 后端A | M0 | FR-6 |
| WriterAgent + SSE 流式 | 后端A | StateStore | FR-2 |
| 分歧点检测 + 卡池生成（方向生成器） | 后端B | WriterAgent | FR-3/4 |
| 抽卡引擎（盲抽/明选/权重/稀有度） | 后端B | 卡池生成 | FR-4 |
| 自由输入意图路由 | 后端B | 卡池生成 | FR-5 |
| ConsistencyChecker + 伏笔 + 时间线 | 后端B | StateStore | FR-6 |
| 去 AI 味 lint + 文风预设 | 后端B | 生成 | FR-7 |
| 审稿-修复 Agent | 后端B | 一致性 | FR-12 |
| 回溯/分支 API | 后端A | DecisionLog | FR-9 |
| 前端脚手架 + 设计系统 | 前端 | M0 | — |
| 决策页（抽卡动画/输入窗） | 前端 | M3 API | FR-11 |
| 正文流式页 + 设定/图鉴/分支视图 | 前端 | M2/M4 API | FR-11 |
| 自动化测试 + 提测基线执行 | QA | 各 M 结束 | 全体 |
| 验收签批 / UAT | PM + 产品 | M6 | 全体 |

---

## 3. 核心玩法验收清单（M3 Gate，抽卡/自输主路径）

自动化 + 冒烟脚本逐条执行，任一失败不关闭 M3：
1. 分歧点出现后，前端收到 `decision_point` 事件且正文流停止。
2. 明选：展示 3–5 张卡，点选 → `apply(card_id)` → 生成正文与卡方向一致。
3. 盲抽：`gacha` 返回一张随机卡（同卡池连续 100 次有 ≥2 种卡面），揭晓动画状态正确。
4. 稀有度合法：产出卡 rarity ∈ {N,R,SR,SSR}，SSR 必带 `risk_balance`。
5. 自由输入三种典型意图（加事件/加角色/切场景）各自能生效并更新状态。
6. 改写卡：`custom_transform` 后采用，正文体现改写内容。
7. 决策节点锁定：`apply` 二次调用返回 409。
8. 一轮完整闭环：生成→分歧→决策→生成 ≤ 40s（正常网络，模型非限流）。

---

## 4. 质量门禁与测试基线

### 4.1 提测标准（经理放行/QA 提测的 Gate）
- CI 全绿：lint + 类型检查 + 单测 + 接口契约校验（schemathesis/OpenAPI diff 通过）。
- **接口契约回归**：所有已签发端点字段与《接口契约》一致（新增/变更必须走契约变更流程）。
- **异常场景覆盖率 ≥ 80%**：对每个接口，非法参数、越权、重复操作（409）、上游失败（502/429）、超长文本、空请求等异常 case 全覆盖且通过。此为用户交互可靠性底线，防止"开发完才发现不满需求"。

### 4.2 测试层级与策略
| 层 | 工具 | 场景 | 通过标准 |
|----|------|------|----------|
| 单元 | pytest / vitest | 意图路由、卡权重、压缩、去AI味规则、错误码 | 覆盖率 ≥ 70%（新逻辑） |
| 集成 | pytest + TestClient | Gateway 路由、StateStore 一致性、SSE 顺序 | 关键路径全绿 |
| 契约 | schemathesis / openapi-diff | API Spec 与实现一致性 | **diff 为空** |
| 端到端 | Playwright | 决策页、正文流式、分支 VS 跳 | 核心路径 8 条通过 |
| LLM 评估集 | 固定 golden 用例 + 自动 judge | 去AI味命中、卡面相关度、方向一致性、吃书检测 | 见 §4.3 |

### 4.3 LLM 质量回归基线（相对主观项的最小客观底线）
维护一个约 30 条的评测集（含：吃书样例、伏笔回收、卡面跑题、风险平衡丢卡、去AI味 0 分项）。每次模型/提示词改动跑回归：
- 一致性（吃书）错误率 ≤ 10%。
- 卡面与决策点相关度人工抽查 ≥ 4/5 相关。
- 去 AI 味 lint 阻断级命中 ≤ 5 条 / 30 章。
- 生成方向与所选卡/输入一致率 ≥ 90%（自动化 LLM-judge 判定）。

### 4.4 发布门禁（Go/No-Go）
- P0 无未关闭阻挡项；
- §4.1–4.3 全绿；
- 跨 30 章的"重大矛盾"≤ 2 处（人工走查）；
- 抽样问卷满意度 ≥ 4/5；
- 决策交互时长 ≤ 30s。

---

## 5. 依赖关系与风险计划
| 风险 | 缓解 | 责任人（截止节点） |
|------|------|------|
| 核心玩法延迟（M3 推迟挤压前端） | 前端先做数据 mock（基于契约）；提供契约 mock server | 后端A |
| 模型服务商不稳定拖慢 demo | LLM Gateway 多提供商 + 本地模型 fallback | 后端A（M2） |
| 抽卡断线感（高戏剧卡） | 提前在 M3 引入 `risk_balance` + 兜底 Agent | 后端B（D20 前） |
| 需求蔓延 | scope 栏（见需求文档 §6）×评审；新需求进 backlog 不进当前 Milestone | PM |

---

## 6. 缺陷管理（提测后的 Bug 流程，配合《工程规范》§5）
- 每个 Bug 带：严重级（P0 阻塞/P1 主要/P2 一般/P3 建议）、复现步骤、受影响接口、期望行为。
- P0 阻断对应 Gate；P1 可在本 Milestone 内修复后放行；P2/P3 进 backlog。
- 流程状态机：`New → Triaged(定级/指派) → In Progress → Fixed(提交) → Validate(QA) → Closed/Reopened`。