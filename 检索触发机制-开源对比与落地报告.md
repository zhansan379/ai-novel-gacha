# 检索触发机制：开源 AI 小说项目对比与 choice_novel 落地报告

> 本文对比 13 个开源 AI 小说项目与 choice_novel 自身对「**生成时是否需要网络搜索 / 设定召回（RAG），以及按什么触发**」的处理，评估 choice_novel 新增「一次 LLM 判定检索画像 + 分层检索」在开源生态中的位置与差距。

---

## 1. 背景：要解决的问题

生成小说时，AI 最大两类硬伤：
- **事实错**（历史/军事/医疗/法律题材编造细节）；
- **前后矛盾**（角色、伏笔、时间线、设定不一致）。

网络搜索解决前者，设定召回解决后者。但「要不要搜、搜什么」不能一刀切，需要按四个维度判断：

| 维度 | 判据 | 典型触发 |
|---|---|---|
| 真实事实密度 | 涉及真实历史/地理/人物/事件/科技 | 历史、传记、地域纪实 |
| 专业深度 | 涉及专业流程/术语/规程 | 医学、法律、军事、金融、采掘 |
| 时效性 | 依赖「当下正在变的现实」 | 最新政策、在售产品、时事、热搜 |
| 设定连续性 | 长篇/系列/架空需前后一致 | 所有长篇、群像、伏笔书 |

**本文关注点**：开源项目里，谁做了网络搜索、谁做了召回、谁做了「按维度/类型决定何时搜、搜什么」——以及 choice_novel 的实现处于什么位置。

---

## 2. choice_novel 自身实现（本次新增）

### 2.1 架构
开书一次 LLM 判定 → 产出「检索画像」→ 按画像并行预取 → 正文仅时效性维度轻联网。彻底移除旧的「提取英文 token 再联网」启发式。

| 阶段 | 动作 | 关键文件:行 |
|---|---|---|
| 开书·波1 | `ProfileDeterminer.determine()` 一次 LLM 判定四维度 + 中文专题 | `backend/app/services/retrieval.py`；`story_service.py:187-197` |
| 开书·波2 | `prefetch()` 按画像 topics 网络**并行预取**并入 `story.grounding` | `story_service.py:210` |
| 开书·波1 | `resolve()` 知识库实体召回 | `story_service.py:187` |
| 正文·每拍 | `_ground_decision()`：离线知识库召回**每拍跑**；**联网仅当** `timeliness=high and require_web and web.enabled` | `story_service.py:362-389` |

### 2.2 触发条件（改造后）
- **网络搜索触发**：不再由文本正则决定，而由画像。开书一次预取；正文仅时效性(high)拍别轻搜一次（短超时、失败静默）。
- **知识库召回触发**：仍按「实体点名」——文本里真正写到某真实实体名（刘强东/京东/…）才注入，纯架空 zero 注入。
- **无 key 降级**：`WebSearch.enabled` 取决于 `search_api_key`；未配 → 网络预取为空、正文不联网，回落到纯召回，行为等同旧系统。

### 2.3 输出画像结构（存 `story.retrieval_profile`）
```jsonc
{
  "real_world": "high",
  "profession": {"level": "high", "domains": ["采掘", "安全规程"]},
  "timeliness": "low",
  "continuity": "high",
  "topics": [{"label": "煤矿井工开采主要流程", "kind": "professional"}],
  "require_web": true
}
```

---

## 3. 开源项目全景（13 个）

### 3.1 denova（Go / 内置 Agents·Skills·SubAgent）
- **网络搜索** ✅ `web_search`+`web_fetch`，SearXNG 优先、DuckDuckGo/Bing 并发兜底；由 LLM 在生成中**自主**决定调用哪个 agent 工具。
- **RAG/向量** ❌ 无向量；资料库(lore)用**关键词/倒排**召回，另有 `resident/auto/manual` 三模式。
- **设定召回** ✅ resident 常驻全量注入 + auto/manual 工具检索。
- **类型差异化** ❌ 全球统一，genre 仅用于交互事件预设。
- **一致性核查** ❌ 基本无正文一致性引擎。
- **定位**：同时拥有网搜与资料库，但都由 agent 自主选择、不按类型分流，正文一致性弱。

### 3.2 kimi-writer（Python / kimi-k2-thinking 自主 agent）
- **网络搜索** ❌ 无（唯一 HTTP 是 token 估算接口）。
- **RAG** ❌ 无。
- **设定召回** 仅在上下文超限时把早期消息**压缩成摘要**（全量→摘要），非检索式。
- **类型差异化** ❌ **一致性核查** ❌。
- **定位**：纯 prompt 写作 agent，两者皆无，最具雏形。

### 3.3 storyforge（TS/Vue）
- **网络搜索** ❌ 无。
- **RAG** ✅ 完备：embedding 适配器 + `retrievalChunks` 向量 + `canon-provider` 字段级策略；主检索实为关键词+摘要，向量供语义评审用。
- **设定召回** ✅ 多层检索注入：章节切块/摘要/**直接连续性**/长期档案(`consistency-dossier`)/时间线/知识台账；must-read·pinned 常驻。
- **类型差异化** ⚠️ 按**任务类型 task kind**（prose/outline/character…）分流上下文预算，非小说品类。
- **一致性核查** ✅ **最强**：`prose-semantic-review`（world-rule-conflict/continuity-conflict/pov-knowledge-leak/future-plot-leak/outline-deviation/character-motivation-break + blocking/warning）+ `consistency-agent`（held-items/knowledge-ledger/lifecycle-boundary）。
- **定位**：零网搜，设定召回与一致性核查最深。

### 3.4 OpenFic（Python/FastAPI）
- **网络搜索** ✅ 9 个 provider（brave/serper/searxng/tavily/ddgs/exa/jina/perplexity/zhipu），LLM 在 ReAct 循环里按需调用，默认关。
- **RAG** ✅ 章节向量检索（embedding + 可选 rerank + 混合 RRF），freshness 门控。
- **设定召回** 固定窗口章节上下文（近 9 章原文 + 远期摘要）为主；世界书关键词子串检索。
- **类型差异化** ❌ **一致性核查** ⚠️ web_search 自称用于事实核查，靠 agent 自觉调用 + auditor/reviewer 子 agent，无自动后处理。
- **定位**：网搜+向量检索都有，agent 自主触发，无类型分流、无自动事实核查。

### 3.5 AI-Novel-Writing-Assistant（Biz Novel Studio）
- **网络搜索** ❌ 生成期无；仅独立市场榜单爬虫（番茄/起点/晋江）做市场雷达。
- **RAG** ✅ **最强**：Qdrant 向量+关键词+RRF+reranker，chunk facet（genreTags 等）；每章按**任务感知 query**（mission/目标/角色）自动召回。
- **设定召回** ✅ 每章使命感知 RAG 检索 + `completedMilestones` 不可逆事实账本注入。
- **类型差异化** ⚠️ 检索全局统一；genreTags 仅作 facet（书分析场景）。
- **一致性核查** ✅ 章节评审 + AuditService + 时间线一致性 checker。
- **定位**：无生成期网搜，Qdrant 混合 RAG 最强，任务感知召回+评审。

### 3.6 AI-Novel-Writer（electron/TS 桌面）
- **网络搜索** ❌ 无。
- **RAG** ✅ 本地 vector-store(l2) + 全文检索；`search_knowledge` 暴露给 agent。
- **设定召回** ✅ L0(L500)–L1(编辑器感知)–L2(agent 按需 read_*/search_knowledge) 三级注入。
- **类型差异化** ❌ **一致性核查** ⚠️ 仅生成前 `continuity-preflight` 规则校验。
- **定位**：无网搜，本地向量知识库语义检索，agent 按需取用。

### 3.7 NovelClaw（Python/FastAPI）
- **网络搜索** ❌ 无。
- **RAG** ✅ ChromaDB 双库：记忆系统（文本/大纲/人物/世界观/情节点/事实卡向量化）+ 静态知识库（导入语料按 genre 过滤）。
- **设定召回** ✅ 混合：无条件注入滚动摘要/最近Fact + 向量+词法检索补人物/大纲/世界观/伏笔。
- **类型差异化** ⚠️ genre 仅作知识库过滤条件，检索方式统一。
- **一致性核查** ✅ `check_character/plot/world` + `comprehensive_check`（LLM 对照检索记忆评测）。
- **定位**：纯设定召回路线，双库 + 专门一致性检查器，零网搜。

### 3.8 oh-story-claudecode（Claude 技能套件）
- **网络搜索** ✅ **按需 researcher agent**：遇到需查证的外部事实（历史制度/地理/职业/风俗）暂停写作、spawn `story-researcher` 用 CDP 搜 Google/Bing + WebSearch/webReader，带来源存档；另 `story-long-scan` 用 browser-cdp 扫榜做市场研究。
- **RAG** ❌ 无数值向量；知识体系为静态 markdown + 「按需加载」。
- **设定召回** ✅ 文件式**预算召回**：每章只读「不知道就会写错」的角色状态/待收伏笔/相关设定；跨书/文风/情绪分项召回；题材卡 30+ 细分。
- **类型差异化** ⚠️ 内容层题材卡/数据源选择（30+ 细分映射），非自动路由。
- **一致性核查** ✅ story-review + story-deslop。
- **定位**：网搜做成「需查证才搜」的独立 researcher agent + 文件预算召回，触发设计最接近按需查证。

### 3.9 chinese-novelist-skill（中文小说技能包）
- **网络搜索** ❌ **RAG** ❌。
- **设定召回** 全量灌大纲/人物档案/文风基准/上一章结尾 + 「设定词典」维护专有名词一致性。
- **类型差异化** ❌ phase1 问询定位，生成统一。
- **一致性核查** ⚠️ phase4 字数/质量自动校验、不合格自动重写（最多 3 轮）。
- **定位**：二者皆无，纯 prompt 全量灌入 + 设定词典。

### 3.10 neuro-book（Bun/TS）
- **网络搜索** ❌ 无。
- **RAG** ✅ **自研 nb-memory**：bi-temporal 双时间轴（tick 摄入序 + instant 故事时间），embedding + BM25 RRF + 启发式/LLM 查询规划器，as-of 时间点查询**防未来信息泄漏**（基准时间泄漏率 0%）。
- **设定召回** ✅ 检索式记忆引擎（当前为独立框架，未观察到接入生成 prompt 的调用链）。
- **类型差异化** ❌ **一致性核查** ⚠️ 引擎本身以防时间倒灌为目标；`llmlint` 做风格/AI 味检测（非一致性）。
- **定位**：自研 bi-temporal 记忆+检索引擎专攻长文一致性与时间线防泄漏，尚未接进写作链路。

### 3.11 terminal-velocity
- 是一本由 10 个 AI agent（KinOS v6）独立写成的 10 万字完稿**成书产物**，非可复用工具；`ResearcherAgent` 曾联网做设定验证，属产物侧一次性行为。

### 3.12 motion-ref-skill / ui-craft
- 前端设计技能，与小说生成无关。

---

## 4. 横向对比矩阵

| 项目 | 网络搜索 | RAG/向量 | 设定/前文召回 | 按类型/维度分策略 | 生成后核查 |
|---|---|---|---|---|---|
| **choice_novel**（本次） | ✅ Tavily，画像驱动 | ✅ Chroma 实体召回 | ⚠️ facts 全量注入 | ✅ **开书一次四维度画像判定** | ⚠️ 设定自检 |
| denova | ✅ agent自主 | ❌ 关键词 | ✅ resident+工具 | ❌ | ❌ |
| kimi-writer | ❌ | ❌ | ⚠️ 摘要压缩 | ❌ | ❌ |
| storyforge | ❌ | ✅ 强 | ✅ 多层检索 | ⚠️ 按任务类型 | ✅ **最强** |
| OpenFic | ✅ 9 provider | ✅ | ⚠️ 窗口注入 | ❌ | ⚠️ 无自动 |
| Biz Novel Studio | ❌ 生成期 | ✅ **最强** | ✅ 任务感知 | ⚠️ genre facet | ✅ |
| AI-Novel-Writer | ❌ | ✅ | ✅ L0/L1/L2 | ❌ | ⚠️ 前置校验 |
| NovelClaw | ❌ | ✅ | ✅ 混合 | ⚠️ genre 过滤 | ✅ |
| oh-story | ✅ 按需researcher | ❌ | ✅ 文件预算 | ⚠️ 题材卡 | ✅ |
| chinese-novelist | ❌ | ❌ | ⚠️ 全量灌入 | ❌ | ⚠️ 质量校验 |
| neuro-book | ❌ | ✅ 独特bi-temporal | ✅ 检索式 | ❌ | ⚠️ 防泄漏 |

---

## 5. 按四维度：谁最强、谁缺席

- **真实事实 → 网搜**：`oh-story` 最接近（遇到需查证事实暂停写作、联网查证、带来源存档）；`OpenFic`（9 provider）/`denova` 有工具但无「该查证才查」触发。**无项目校验网络返回的真实性/权威性**。
- **专业深度 → 权威资料**：仅 `NovelClaw` 的静态知识库支持导入专业语料按 genre 过滤；其余无「教材/论文/手册」专业层。
- **时效性 → 实时搜索**：`OpenFic`/`oh-story`/`denova` 能拿实时信息，但**无时效性触发判断**；`choice_novel` 是唯一把 `timeliness` 写进判定、且只对该维度开放正文联网的。
- **设定连续性 → 召回**：`storyforge`、`Biz`、`NovelClaw`、`neuro-book` 最深；尤其 `neuro-book` 的**时间线防未来泄漏**与 `storyforge` 的**伏笔/持有物/认知边界一致性 agent** 直击长篇连续性痛点。

---

## 6. choice_novel 与开源的差距与可借鉴

**独有优势**：choice_novel 是**唯一把「要不要搜索、搜什么」做成开书一次结构化四维度判定的项目**。其余项目要么「agent 自主决定调不调工具」（denova/OpenFic），要么「关着网不搜」（storyforge/Biz/NovelClaw/Writer/neuro-book），要么「仅题材卡做内容层切换」（oh-story）。没有谁在生产链路里按维度做一次画像判定后据此路由。

**三大差距（可落地改进）**：
1. **设定/前文召回仍是全量注入**：`facts.py` 把角色/伏笔/规则/关系整份塞 prompt，长篇越写越大。而 `storyforge`/`Biz` 已是检索式、`neuro-book` 还能防时间泄漏。→ 下一步可把 facts 改成长度/相关性裁剪的检索注入。
2. **一致性核查只有 LLM 自检，无外部交叉**：`storyforge` 的语义评审代码集（world-rule-conflict/pov-knowledge-leak…）与 `NovelClaw` 多维度一致性检查器更硬。→ 可把 consistency 从「passed/issues」升级为题写分类评审。
3. **网络数据源单一（Tavily）**：`OpenFic` 有 9 个 provider。→ 若需可扩展 provider。

**一句话结论**：choice_novel 已站在开源生态的空白点上——「按维度决策检索」目前无人做全；设召回与生成后一致性核查则是开源强项的领域，可作为下一步对标方向。

---

## 7. 附：本项目本次落地改动清单
- 新增 `backend/app/services/retrieval.py`（RetrievalProfile / ProfileDeterminer / prefetch）。
- 改造 `grounding.py`（收 `resolve()` 为纯实体召回、删拉丁 token 联网）。
- 接入 `story_service.py`（波1 判画像、波2 预取、`_ground_decision` 时效性联网）。
- 持久化 `Story.retrieval_profile`（store.py / sqlite.py 快照导入导出）。
- 配置 `config.py`（profiling_enabled / profiling_max_topics）。
- 测试 `tests/test_retrieval_profile.py` 新增；`tests/conftest.py` 关闭测试期联网。