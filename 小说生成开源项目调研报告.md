# 小说生成开源项目调研报告

> 调研时间：2026-09-20
> 调研对象：GitHub 上 Star > 200 的小说生成相关开源项目（共 11 个）
> 所有项目均已浅克隆（`git clone --depth 1`）到本目录 `novel_projects/` 下，报告基于克隆的实际源码 + README + 文档整理。

## 目录

1. [项目清单](#一项目清单)
2. [横向对比总表](#二横向对比总表)
3. [逐项目详解](#三逐项目详解)
4. [共性实现机制分析](#四共性实现机制分析)
5. [选型建议](#五选型建议)

---

## 一、项目清单

搜索关键词：`novel generation` / `ai novel writing` / `小说写作` / `网文` / `story generation`。排除论文合集与无关项目后，Star > 200 的完整工具/框架共 11 个：

| # | 项目 | Star | 形态 | License |
|---|------|-----|------|---------|
| 1 | zenstory-ai/oh-story-claudecode | ~7000 | 编程 Agent skill 套件 | MIT |
| 2 | PenglongHuang/chinese-novelist-skill | ~3100 | Claude Code skill | 开源免费 |
| 3 | ExplosiveCoderflome/AI-Novel-Writing-Assistant | ~3000 | 全栈 Monorepo + 桌面 | AGPL-3.0 |
| 4 | mind-protocol/terminal-velocity | ~1100 | 手稿 + 过程档案 | 开源 |
| 5 | syrizelink/OpenFic | ~1100 | 全栈 + 桌面 + Docker | 开源 |
| 6 | EthanYoQ/AI-Novel-Writer | ~1000 | Electron 桌面 | GPL-3.0 |
| 7 | alfredxw/denova | ~780 | 全栈 IDE + Agent 平台 | Apache-2.0 |
| 8 | yuanbw2025/storyforge | ~740 | 纯前端本地优先 | MIT |
| 9 | notnotype/neuro-book | ~680 | Nuxt + 多包工作区 | AGPL-3.0 |
| 10 | Doriandarko/kimi-writer | ~590 | Python CLI | MIT |
| 11 | iLearn-Lab/NovelClaw | ~370 | FastAPI 工作台 | MIT |

---

## 二、横向对比总表

| 项目 | 后端 | 前端/桌面 | AI 编排 | 记忆/一致性 | RAG/向量 | 去 AI 味 | UI | 部署难度 / Key |
|------|------|-----------|---------|-------------|----------|----------|-----|----------------|
| oh-story-claudecode | Node/Python 脚本 | 无独立 UI（本地 dashboard） | 宿主 Agent 7 sub-agent + hooks | 文件系统当记忆 + 12KB 状态卡 + 伏笔/双时间线 | 无 | 确定性 lint | 部分 | 免 key，装进 Agent |
| chinese-novelist-skill | 无 | 无 | Claude 原生 subagent/teams | 偏好 JSON + 大纲/摘要 | 无 | 润色步骤 + 字数脚本 | 无 | 免 key |
| AI-Novel-Writing-Assistant | Express+Prisma+SQLite | React19 / Electron | LangGraph + Agent Runtime | 自动导演 + 状态回灌 + 角色账本 | Qdrant | 写法引擎 + 反 AI 规则 | 完整 Web+桌面 | 需 LLM key |
| terminal-velocity | KinOS（外部） | 无 | KinOS 自主 Agent 团队 | 角色/时间线文件 + Agent 分工 | 无 | Agent 内部 | 无 | 非独立部署 |
| OpenFic | Python FastAPI + SQLite | React+TipTap / Electron | LangGraph 1.x ReAct + YAML Agent | 上下文压缩 + 章节摘要 | LanceDB Agentic RAG | 内置写作方法论/禁词表 | 完整 Web+桌面+PWA | Docker/pip/桌面 |
| AI-Novel-Writer | better-sqlite3（主进程） | React19 + Monaco | ReAct Agent + 工具注册 | L0/L1/L2 上下文 + 来源明确的连续性 | LanceDB（退化 SQLite FTS） | 文风分析 + skill | 完整桌面 GUI | 免/填 key，直接装 |
| denova | Go 1.26 + hertz | React + Vite (PWA) | 模块化 Agent 运行时 + 6 写作 SubAgent | CREATOR.md + outline/progress/character 状态文件 | 文件式 | 弱（风格预设） | 全 Web | 需多个模型 key |
| storyforge | 无（纯前端） | React19 + Vite + tiptap | Prompt 任务适配器 | chapter-memory 分层注入 | 检索 + continuity-envelope | 强（anti-ai / writing-styles） | 全 Web | BYOK，无后端 |
| neuro-book | Nuxt/Nitro + Bun | Nuxt + Vue + Electron/Tauri | pi-agent-core 多 Agent | World Engine + 伏笔账本 + nb-memory | sqlite-vec | 最强（llmlint 360 条） | Web+桌面 | 自配 key，Windows 免装 |
| kimi-writer | 无 | 无 | 单 Agent ReAct，3 工具 | 仅对话内压缩摘要 | 无 | 无 | 无（CLI） | Moonshot key |
| NovelClaw | Python FastAPI + SQLite | 服务端渲染 Web 模板 | 9 种 Agent + Claw 循环 | 16 个 memory banks | chromadb + sentence-transformers | 中（style/judge/evaluator） | Web 模板 | Docker，可选 key |

**一句话横向小结**：这 11 个项目可以分成三大路线——

- **轻量 skill 路线**：`oh-story-claudecode`（长篇连续性最强）、`chinese-novelist-skill`（最简单），复用已装好的编程 Agent / Claude Code 能力，无独立后端。
- **功能完备创作工作台**：`AI-Novel-Writing-Assistant`（自动化与衍生能力最全）、`OpenFic`（基础设施最现代）、`AI-Novel-Writer`（最本地优先）、`denova`（最接近完整写作 IDE）、`storyforge`（纯前端最易部署）、`neuro-book`（工程化/去 AI 味最强）、`NovelClaw`（记忆账本 + 可观测性最强）。
- **实验/量产工具**：`terminal-velocity`（全自主 Agent 创作小说并出版的实验存档，工具属性最弱）、`kimi-writer`（一键挂机量产 CLI）。

---

## 三、逐项目详解

### 1. oh-story-claudecode（ZenStory AI，~7000 star）

**磁盘路径**：`novel_projects/oh-story-claudecode`

**核心定位**：网文写作 skill 套件——以 13 个 skill 的形式"装进"你已使用的编程 Agent（Claude Code、Codex、ZCode、OpenCode 等 8 种宿主），打通「扫榜→拆文→搭纲→写正文→去 AI 味→生成封面」的网络小说全流程，主打商业化网文与长篇连续性。

**技术栈**：产品本体是 Markdown（写作方法论与工作流契约）+ Node/Python 确定性检查脚本，**不是运行时程序**，无需 GPU、无需自配模型（直接用宿主 Agent 的模型）；仅封面生成单独调 GPT-Image-2。

**目录结构**：`skills/`（13 个 skill，每含 SKILL.md + references 知识库）、`docs/`（架构/去 AI 味/连续性方法论）、`scripts/`（CI 守卫与测试）、`demo/`、`tests/`、`.claude-plugin/`、`.zcode-plugin/`。

**核心实现机制**：
- **文件系统当记忆**：每本书拆成 `设定/大纲/正文/追踪/对标/参考资料` 独立目录；`追踪/_tracking-state.json` 是唯一结构化权威，派生上下文卡、伏笔视图、角色状态、`作者真相/读者已知`双时间线。续写状态卡固定 7 栏、硬上限 12KB，不进正文 prompt；逐章记录 ≤3072 bytes。
- **7 个专业 Agent 分工**：story-architect（架构）、narrative-writer（正文）、consistency-checker（一致性/伏笔）、character-designer、story-researcher（联网）、story-explorer、chapter-extractor。
- **8 个自动化 hook 守质量**：其中仅 `guard-outline-before-prose.sh`（写正文前必须有细纲）是阻断性的，其余只提示。
- **去 AI 味**：`/story-deslop` 是确定性本地写作 lint，逐条匹配已知 AI 句式/标点/退化模式（check-ai-patterns.js、check-degeneration.js、normalize-punctuation），给出阻断/建议分级。
- **无 RAG / 无向量库**，不靠对话记忆，靠文件系统 + 符号状态保证连续性，是本项目最深的机制。

**运行方式**：`npx skills add zenstory-ai/oh-story-claudecode -y -g` 安装，然后在写作项目根运行 `/story-setup` 部署 hooks/agents/references。不需要额外 API key。

**成熟度**：工程化与 CI 极强、中文网文商业化取向（起点/番茄/晋江/知乎盐言）、长篇连续性机制同类最深入。局限：高度依赖编程 Agent 宿主、无独立云端 Web。

---

### 2. chinese-novelist-skill（PenglongHuang，~3100 star）

**磁盘路径**：`novel_projects/chinese-novelist-skill`

**核心定位**：面向"坚持写完一本完整中文小说"的 Claude Code skill（v2.0），核心卖点：三层递进式问答、偏好记忆、中断续写、三种写作模式、自动校验。

**技术栈**：Claude Code skill（SKILL.md + references 流程/写作指南 + 1 个 Python 字数脚本），纯提示词 + 分阶段工作流驱动，无独立后端/API/UI。

**核心实现机制**：
- **分阶段流程**：Phase 0 初始化（加载偏好、检测中断续写）→ Phase 1 三层问答（L1 核心定位 3 问必答 + L2 深度定制 5 问可选 + 标题生成）→ Phase 2 规划确认（7 列章节大纲 + 人物档案 + 写作计划 JSON）→ Phase 2.5 选写作模式（串行 / 子 Agent 并行 / Agent Teams）→ Phase 3 疯狂创作（全自动逐章：写前分析→撰写 3000~5000 字→润色去 AI 味→字数检查→更新摘要）→ Phase 4 自动校验（字数/连贯性，不合格最多自动重写 3 轮）。
- **记忆/一致性**：跨会话偏好 `user-preferences.json`；每章重读大纲对应规划并维护摘要；"每章必爽/悬念承上启下"等黄金法则。
- **无 RAG / 无向量 / 无编排库**，全部靠 Claude 能力。

**运行方式**：放 `~/.claude/skills/` 或 `npx skills add`；用 Claude Code 模型，无独立 UI，免额外 key。

**成熟度**：轻量、中文友好、聚焦"拖稿难"痛点、上手极快。局限：强依赖 Claude Code、机制较浅，适合中小型/首部小说。

---

### 3. AI-Novel-Writing-Assistant（Biz Novel Studio，~3000 star）

**磁盘路径**：`novel_projects/AI-Novel-Writing-Assistant`

**核心定位**：面向"完全不懂写作的新手"的 **AI 导演式长篇生产系统**——一句灵感开整本书，自动导演 + 整本生产主链 + 写法引擎 + 本书世界 + RAG，把章节生成/审核/修复/状态回灌串成可暂停可恢复的生产链，并有漫画/短剧衍生工坊。

**技术栈**：pnpm monorepo；前端 React 19 + Vite + Pluto（编辑器）+ TanStack Query；后端 Express 5 + Prisma + Zod；AI 编排 LangChain + LangGraph；数据库 SQLite（Prisma 可切 PostgreSQL）；RAG Qdrant；桌面 Electron。

**核心实现机制**：
- **多提供商 LLM 路由**：OpenAI / DeepSeek（默认）/ Anthropic / SiliconFlow / xAI，按任务（planner/writer/review/repair/summary…）分别路由，温度按任务给定（如 planner 0.3、writer 0.8、review 0.2）。
- **自动导演开书**：从一句灵感生成多套整本方向+标题组 → 书级方向 → 角色 → 卷战略 → 节奏板 → 章节清单 → 章节执行，全自动中途若遇模型不可用/配额/连续失败会主动停机并从检查点恢复。
- **章节执行主链**：正文生成 → AI 审核 → 修复 → 质量债记录 → 角色状态/事实/伏笔回灌 → 下一章入口；角色"资源账本"按章节参与者精准筛选，避免把全部角色塞进 prompt。
- **RAG**：Qdrant + Embedding（流式并行索引、chunk hash 去重、检索 trace），拆书结论进 facets 索引。
- **写法引擎 + 反 AI 规则**：可保存/编辑/绑定/复用的写法资产；从现有文本提取写法特征 + 原文样本形成特征池；反 AI 规则减少模板感/解释感。

**运行方式**：Node ^20.19 + pnpm ≥10.6；`cp server/.env .`、`pnpm dev`（前端 5173 / 后端 3000）；需至少一个 LLM API key，RAG 需 Qdrant（可 `RAG_ENABLED=false` 跳过）；有 Dockerfile，桌面 Release 可直接下载。

**成熟度**：功能最全、深度最成熟、中文友好、有桌面端。局限：**AGPL-3.0 双许可**（商用 SaaS 需向作者授权）、依赖栈重、部署复杂。

---

### 4. terminal-velocity（mind-protocol，~1100 star）

**磁盘路径**：`novel_projects/terminal-velocity`

**核心定位**："全球第一部由自主 AI 代理团队写成并出版的小说"（约 10 万字，已上架 Amazon）。仓库本身更偏向**产物 + 创作过程档案**，真正的运行核心是外部的 **KinOS (v6)** 自主代理操作系统。

**技术栈**：KinOS（基于 Claude-3，独立仓库）；本仓库内仅有少量 Python 辅助脚本 + 文档 + 手稿，**无应用代码 / 无 UI**。

**目录结构**：`complete_manuscript.md/.txt`（成品手稿）、`story/`（act1-4、scenes、structure、master_timeline.md）、`characters/`、`world_building/`（含 tech-tree.yml）、`documentation/`、`scripts/`、`monitoring/`。

**核心实现机制**：10 个专用 Agent 团队（Specifications / Production / Management / Evaluation / Chronicler / Documentalist / Deduplication / Redundancy / Integration / Writing + Researcher），多 Agent 自主协作、无人工干预，全程直播 + GitHub commit 透明。一致性靠角色文件、world_building、全局时间线 + Agent 分工去重纠错，无现代 RAG。

**运行方式**：作为手稿可直接阅读；创作侧由 KinOS 运行，本仓库不做独立部署。

**成熟度**：实验性与叙事价值显著（已出版、社区关注）。局限：可复用/工程化很低，核心代码在外部仓库，更像实验记录而非工具。

---

### 5. OpenFic（syrizelink，~1100 star）

**磁盘路径**：`novel_projects/OpenFic`

**核心定位**：跨平台、本地优先的 AI-Native **Vibe Writing** 一站式小说写作工具——让 Agent 深度参与设定/角色/伏笔维护、发散思路、协同编辑，而非"一键生成整本"；允许按你的写作流程高度定制 Prompt、Agent 与工作流。

**技术栈**：后端 Python 3.12 + FastAPI + LangChain/LangGraph 1.x（LangGraph-checkpoint-sqlite）；前端 React + TipTap + TanStack Query；桌面 Electron；数据库 SQLite（SQLModel/Alchemy + alembic）；RAG LanceDB（IVF-HNSW + FTS + BTree 混合）+ fastembed 本地嵌入；模型经 models.dev 目录。

**核心实现机制**：
- **ReAct 子图工厂**：每个 Agent 是一个独立编译的 LangGraph StateGraph loop；统一超时/重试/错误分类入口。
- **内置 Agent 以 YAML 定义**（writer、reviewer、plan、auditor、actor、composer…），内含大量中文写作方法论（白描禁修辞、禁词表、"活人感写作"、反裸时间词开场）。
- **上下文管理**：多层上下文 + 自动压缩窗口 + 动态截断 + 稳定缓存；独立 memory 系统做章节摘要。
- **"Agentic RAG"**：LanceDB 向量化检索 + FTS，目标支持百万字级项目内高效召回；本地数据优先（零云存储）。
- **多模型 + 成本优先**：OpenAI/Anthropic/Google/Groq/Mistral/DeepSeek/OpenRouter/Ollama + 任意 OpenAI 兼容端点。

**运行方式**：`docker run -d -p 8000:8000 ghcr.io/syrizelink/openfic:latest`，或 `pip install openfic && openfic serve`，或直接下载桌面 Release；模型 key 界面配置。

**成熟度**：工程成熟、功能完整、开源基础设施最现代（LangGraph 1.x + LanceDB + Pydantic v2）。局限：学习/配置成本较高，不主张一键整本。

---

### 6. AI-Novel-Writer（EthanYoQ，"Vela"，~1000 star）

**磁盘路径**：`novel_projects/AI-Novel-Writer`

**核心定位**：面向 AI 长篇创作的**本地优先 Electron 桌面工作台**，把「前提 → 角色 → 世界观 → 章节蓝图 → 草稿 → 审稿 → 修稿 → 定稿」组织为可追溯流程；数据留在本机。

**技术栈**：Electron + React 19 + TypeScript + Vite；storage better-sqlite3；RAG LanceDB + OpenAI/Gemini embedding；编辑器 Monaco/CodeMirror；状态 zustand。

**核心实现机制**：
- **LLM 协议**：OpenAI-compatible（OpenAI/DeepSeek/Ollama/NovelAI）+ Gemini 原生两类；模型 profile 携带温度；配置存 `~/.vela/models.json`。
- **ReAct Agent**：工具注册 + `<tool_call>` 解析 + observation 循环（MAX_TOOL_ROUNDS=8），15+ 工具（read-blueprint/read-characters/read-drafts/search-knowledge/start-workflow…）。
- **三级上下文注入**：L0 常驻约 500 token（项目总览）→ L1 编辑器感知约 800 token → L2 按需 Tool 调用拉取，含 token 预算保护。
- **长文一致性**：围绕当前章节蓝图 + 相关角色 + 世界观 + 历史摘要组织；**来源明确的连续性材料**（作者填写 / 模型动态状态 / 旧项目未知来源分开）。
- **审稿-修稿闭环**：结构化审稿报告 + 逐项目标审稿，待核实项不视为通过、交作者确认。
- **RAG/知识库**：LanceDB 向量检索；未配置 embedding 时退化为 SQLite FTS 全文检索；可导入多格式参考文本建仿写指南。

**运行方式**：从 GitHub Release 下载安装（Windows .exe / macOS .dmg）；key 界面配置或用 Ollama 本地模型免 key；开发 `pnpm install && pnpm dev`。

**成熟度**：本地优先、隐私安全、模型自由度大、审稿-修稿闭环务实。局限：GPL-3.0、仅 OpenAI-compatible / Gemini、以桌面为主无在线版。

---

### 7. denova（alfredxw，~780 star）

**磁盘路径**：`novel_projects/denova`

**核心定位**：面向小说写作与 AI 角色扮演的一体化 AI 创作平台，内置 AI Agents、Skills、SubAgent 协作、自动化、图像生成与本地版本管理。

**技术栈**：后端 Go 1.26 + cloudwego/hertz（HTTP/SSE），go-git 版本管理、fsnotify；前端 Node 22 + Vite + React（PWA）；图像内置 OpenAI Images/xAI/火山 Seedream/Gemini/ComfyUI；关键依赖 MCP SDK、go-rod（无头浏览器）、go-readability。

**核心实现机制**：
- **模块化 Agent 运行时**：`agent/` 独立 Go 模块，含 model_loop、context（assembler/compaction）、token 估算、providers（anthropicmessages/openaichatcompletions/openairesponses 三种协议协议）。
- **上下文压缩**：独立 `compaction/`（压缩与分组）、`context/`（assembler、状态）、token/input 估算、elision/canonical（关键信息提取）。
- **连贯性**：工作区维护 `CREATOR.md`、`setting/outline.md`、`progress.md`、`character-states.md`，写作时读取并回写更新；有修改审阅、撤销 Agent 改动。
- **6 个写作 SubAgent**：context-planner、writer、reviewer、fixer、final-gate、memory-patcher；支持并行、工具权限、委派、plan mode。
- **Skills**：`skills/` 提供写作工作流（novel-standard：主 Agent 草稿 → SubAgent 审稿 → 修订）。

**运行方式**：`curl .../install.sh | sh`（macOS/Linux）或下载 Release 运行 `denova.exe`；开发需 Go 1.26.6+、Node 22、pnpm、ripgrep。需 API Key。

**成熟度**：工程化程度高、定位接近完整写作 IDE。局限：体积大、上手需配置多个模型、仍在 Beta（v0.4.5）、版本间有数据迁移成本。

---

### 8. storyforge（yuanbw2025 · 故事熔炉，~740 star）

**磁盘路径**：`novel_projects/storyforge`

**核心定位**：开源、本地优先（IndexedDB）的 AI 叙事创作与体验工具，可写长篇/短篇/剧本/漫画，也能做跑团等互动体验。

**技术栈**：纯前端 SPA——TypeScript + React 19 + Vite 6，zustand、react-router、tiptap、Dexie（IndexedDB）、d3/voronoi；无自带后端；AI 调用直接走浏览器请求各服务商 OpenAI 兼容端点。

**核心实现机制**：
- **多提供商**（deepseek/openai/kimi/claude/nvidia/gemini/doubao…），全部走 `/v1` OpenAI 兼容接口；自带本地代理端点解决跨域。
- **上下文/长文**：`context-budget.ts` 内置各模型上下文窗口预设，中文按约 1.5 token/字估算，分层注入 L0 常驻→L1 核心→L2 扩展→L3 增强。
- **连贯性/记忆**：`chapter-memory/` 有 continuity-envelope（连续性核心片段起止注入）、handoff-format（章节交接格式）、canonical-chapter-sequence（原文/事实/摘要/按需检索，找回早期伏笔）。
- **模板引擎**：`prompt-engine.ts` 用 `{{var}}`/`{{#if}}` 模板渲染，庞大种子模板库。
- **去 AI 味**：`anti-ai-adapter.ts` 专门做去 AI 味；`writing-styles.ts` 内置写作风格预设（金庸武侠等）注入 system prompt。
- **生成后写回**：正式创作先产出候选（候选确认制），确认后写回。

**运行方式**：`npm ci && npm run dev`，打开 `http://localhost:5173/storyforge/`；也有在线版（Vercel）。BYOK 填服务商/key/base_URL/模型名。

**成熟度**：成熟度高（3.9.x 版本、丰富测试/CI、多语言 README、B 站教程）、纯前端易部署、附带无需 API 的示例作品《雾港》。局限：纯浏览器跨域需代理、数据按浏览器隔离需导出备份。

---

### 9. neuro-book（notnotype · NeuroBook，~680 star）

**磁盘路径**：`novel_projects/neuro-book`

**核心定位**：用软件工程方法做长篇小说写作的本地 AI 工作区，核心卖点「设定不吃书、伏笔有账本、文字没 AI 味、AI 听指挥」。

**技术栈**：前端 Nuxt 4 + Vue 3 + Pinia + TipTap/Milkdown + Monaco + UnoCSS；后端 Nitro + Express + h3，运行时 Bun；数据 SQLite（libsql）+ Prisma 7 + sqlite-vec（向量）；桌面 Electron + Tauri；Agent 底座 `@earendil-works/pi-ai`、`pi-agent-core`；多包 monorepo。

**核心实现机制**：
- **多智能体**：neuro-agent-harness 提供 Agent 运行时（coordination、approval、compaction、session-transcript）；leader 规划调度、writer 专职正文、researcher 查设定查资料；默认主链：灵感→世界书初始化→World Engine 建档→剧情规划→章节写作→写后回补。
- **World Engine 世界状态引擎（独特）**：用"时间线 + 切面"事件溯源推算任意时刻的世界状态（角色伤势、国库库存）。
- **承诺账本/伏笔系统**：埋下/推进/兑现全程记账，节拍挂场景、情感线提醒；创作决策记录（风险必填、推翻留痕）；章节信息控制（读者/主角知道什么）。
- **去 AI 味（独特）**：`llmlint`——360 条规则对稿件做 lint（填充词、机械过渡、公式化设问、二元对比、空泛总结）+ 静态规则秒级扫全稿 + LLM 规则语境判断 + 自动修复。
- **RAG 记忆**：nb-memory + sqlite-vec 向量库；透明 token 计费。

**运行方式**：Windows 下载 `neuro-book-windows-x64.zip` 运行 `Start Neuro Book.cmd`；Linux/macOS `install.sh`；也有 Docker/GHCR。模型 key 自配（多 Provider）。

**成熟度**：方法论最"工程化"（世界状态引擎、伏笔账本、llmlint 独具一格）、多端支持。局限：AGPL-3.0 强传染、处于快速开发早期（Pre-release）、接口可能不稳定、上手门槛高。

---

### 10. kimi-writer（Doriandarko，~590 star）

**磁盘路径**：`novel_projects/kimi-writer`

**核心定位**：用 Kimi k2-thinking 模型的**自主写作 Agent（CLI）**——输入一个提示词就自动规划并写出完整小说/书。

**技术栈**：极简 Python——单文件 `kimi-writer.py`（约 21KB）+ utils.py + tools/（project/writer/compression）。依赖：openai、httpx、python-dotenv；无框架、无数据库、无 UI。关键参数：模型 kimi-k2-thinking、温度 1.0、每轮 max_tokens 65536、上下文窗口 200000、迭代上限 300、压缩阈值 180000（90%）。

**核心实现机制**：
- **Agentic 循环（ReAct 式）**：模型推理 → decide 调工具 → 结果回填 → 继续到完成或 300 步。
- **3 个工具**：create_project、write_file（create/append/overwrite 三模式写 Markdown 章节）、compress_context（接近 token 上限时自动压缩会话历史）。
- **长文/上下文**：上下文整棵放会话树，逼近 180K token 自动压缩总结；每 50 迭代自动备份 context summary；`--recover` 从快照恢复被打断的工作。无永久记忆库、无 RAG。

**运行方式**：`pip/uv install -r requirements.txt`；`.env` 配 `MOONSHOT_API_KEY`；`python kimi-writer.py "创建…"` 或 `--recover <路径>`。无 Docker。

**成熟度**：极简、零依赖、单文件可读，适合用 Kimi k2-thinking 一键生成全文。局限：只有 CLI 无界面、仅 Moonshot、长文易漂移、纯"一种子 Agent 挂机量产型"。

---

### 11. NovelClaw（iLearn-Lab，~370 star）

**磁盘路径**：`novel_projects/NovelClaw`

**核心定位**：围绕章节写作、运行可观测性、稿件审阅与记忆感知控制构建的结构化长篇小说工作台（FastAPI + 本地 Web），并带公开入口（Portal）与可选的 MultiAgent 快速构思层。

**技术栈**：Python 3.10+ FastAPI + Uvicorn + SQLAlchemy + SQLite；前端 Jinja2 服务端渲染；AI/RAG 依赖 openai、langchain、chromadb、sentence-transformers。三端口：auth-portal（8010）、multiagent（8011）、novelclaw 主工作区（8012）。

**核心实现机制**：
- **LLM 调用**：LLMClient，默认 provider=deepseek（deepseek-chat），也支持 OpenAI/ANTHROPIC/Codex，温度默认 0.7。
- **多智能体**：9 种 agent（writer_agent、plot_agent、world_agent、character_agent、evaluator_agent、judge_agent、idea_copilot_agent、retrieval_agent、base_agent）。
- **ReAct 主循环**：Claw Manager（OpenClaw 风格）——LLM 看全上下文 + 工具，函数调用工具，结果回填直到调用 finalize；ask_user 走文件系统 IPC。
- **RAG/记忆（强）**：16 个 memory banks（session_profile、story_premise、style_guide、scene_cards、entity_state、relationship_state、world_state、continuity_facts、decision_log、revision_notes…）+ chromadb 向量库；一致性检查器、转折点追踪器。
- **可观测性**：worker 子进程产出 progress.log 事件（global_outline、chapter_plan、memory_snapshot、character_setting 回写）在 `runs/<run_id>/` 下。

**运行方式**：Docker（推荐，`docker-start.bat` / `./docker-start.sh`）；本地 `START_LOCAL.bat`。真实调云端 provider 才需 API Key，界面浏览可无 key。

**成熟度**：RAG + 多 Agent + 记忆账本 + 可观测 run 定位清晰。局限：结构较复杂但工程规范性一般（无大型测试套件）、公开版刻意不含真实状态快照、依赖较重（数 GB 的 langchain/chromadb/sentence-transformers）。

---

## 四、共性实现机制分析

横向对比 11 个项目，小说生成领域开源实现呈现出几条高度一致的工程范式：

### 1. 长文/超长上下文是第一难题，几乎全员各有解法
- **上下文层级注入**：几乎所有"工作台"类项目都采用 **L0 常驻 / L1 编辑器感知 / L2 按需拉取** 的多级上下文策略（AI-Novel-Writer、storyforge 尤其典型），配合 token 预算保护，绝不把整本小说塞进 prompt。
- **自动压缩（Compaction）**：kimi-writer（180K 阈值触发压缩）、denova（独立 compaction 模块）、OpenFic（AUTO_TRIGGER_RATIO 自动压缩窗口）、neuro-book（compaction.ts）都实现"接近上下文上限时自动总结历史"。这一点在长篇小说场景几乎成为标配。
- **状态文件 / 账本化**：oh-story-claudecode（tracking-state.json + 12KB 状态卡）、denova（CREATOR.md + outline/progress/character-states）、NovelClaw（16 个 memory banks）把"当前世界状态、角色状态、伏笔、进度"沉淀成结构化文件，写入时读取、写后回写，避免让模型从海量正文里自己推断。

### 2. 从"单次生成"走向"多 Agent 协作 + 生产链"
- **任务分工**：普遍拆分出架构/规划/写作/审稿/修复/一致性检查等角色。oh-story-claudecode 用 7 个 sub-agent；denova 用 6 个 SubAgent；NovelClaw 用 9 种 agent。
- **生产链/瀑布流**：AI-Novel-Writing-Assistant 的"生成→审核→修复→状态回灌→下一章"、neuro-book 的"灵感→世界书→工程→剧情→写作→回补"、Activity 2 的分阶段 Phase 0-4，都把一次创作拆成可暂停、可恢复、可校验的流水线。
- **去 AI 味 / 质量守门**：这是中文网文项目最重视的差异化能力——`oh-story` 的确定性 lint、`neuro-book` 的 360 条 llmlint 规则、`storyforge` 的 anti-ai-adapter + 写作风格预设、`AI-Novel-Writing-Assistant` 的写法引擎 + 反 AI 规则。共同思路是：设定"禁词表 / 机械过渡 / 公式化表达"规则库，对成品做脚本级扫描 + LLM 语境判断，再决定阻断或建议改。

### 3. 记忆/一致性的三条路线
- **文件系统 + 符号状态**（最轻量、无依赖）：oh-story、denova、chinese-novelist-skill。
- **RAG / 向量检索**（medium 以上项目标配）：OpenFic（LanceDB Agentic RAG）、AI-Novel-Writer（LanceDB，退化 FTS）、storyforge（chapter-memory 检索）、NovelClaw（chromadb）、neuro-book（sqlite-vec）、AI-Novel-Writing-Assistant（Qdrant）。
- **"世界状态引擎"事件溯源**（最先进，仅 neuro-book）：用时间线 + 切面推算任意时刻的状态，从根本上解决"设定不吃书/吃书矛盾"。

### 4. "复用宿主 Agent" vs "自建全栈"，是最大的架构分叉
- **skill 路线**（oh-story、chinese-novelist）：复用已装的 Claude Code / Codex / ZCode 的模型与工具能力，几乎零部署、免 key，但受限于宿主、无独立 UI。
- **全栈工作台路线**（其余 9 个）：自建后端 + 前端/桌面 UI + 数据库 + RAG，功能完整、可控，但部署重、需自配 key。

### 5. 模型接入高度统一 → OpenIA 兼容协议
几乎所有项目都以 **OpenAI `/v1` 兼容协议**为默认接入方式（storyforge、denova、AI-Novel-Writer、NovelClaw 的 deepseek 都如此），再扩展原生 Anthropic/Gemini 协议。这意味着项目间的模型可互换性很强。

---

## 五、选型建议

按你的场景快速选择：

- **只是想快速让已用的编程助手写网文** → 用 **oh-story-claudecode**（连续性最强、全流程）或 **chinese-novelist-skill**（最简单）。
- **想要一个开箱即用的完整写作工具 + 界面** → **AI-Novel-Writing-Assistant**（功能最全、自动导演，注意 AGPL 商用授权）或 **storyforge**（纯前端、部署最轻松、中文友好）。
- **在意隐私、数据全在本地** → **AI-Novel-Writer**（Electron 本地优先）或 **storyforge**（IndexedDB）。
- **想要最"工程化"、可审计、去 AI 味最强** → **neuro-book**（世界状态引擎 + 伏笔账本 + llmlint，但处于快速开发期、AGPL）。
- **想要彻底自主挂机量产** → **kimi-writer**（一键 CLI，注意只支持 Moonshot、长文易漂移）。
- **想看多 Agent 全自主创作的标杆实验** → **terminal-velocity**（非工具，是作品 + 过程档案）。

---

*报告由自动化调研生成，基于各仓库克隆源码 + README + 文档整理，细节以各仓库最新源码为准。*