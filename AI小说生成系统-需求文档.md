# AI 小说生成系统 需求文档（PRD）

> 文档版本：v1.0　|　日期：2026-09-20
> 状态：草案，待评审
> 关联：`AI 小说生成系统-设计文档.md`

---

## 1. 项目概述

### 1.1 一句话定位
一个以「**抽卡（Gacha）** / **自由输入**」作为剧情决策核心机制的 **AI 互动小说生成系统**——由用户决定故事的每一次走向，系统基于当前故事状态连续生成剧情连贯、可读、去 AI 味的小说正文，支持长篇连载。

### 1.2 与现有开源项目的差异（差异化依据）
调研了 11 个开源小说生成项目后确认的共性：
- 长文本上下文、一致性、去 AI 味是普遍难题，已有多种成熟解法（多级上下文注入、自动压缩、状态卡、多 Agent 生产链、规则 lint）。
- 但**没有任何项目把"用户如何选择剧情走向"做成核心产品机制**：
  - oh-story-claudecode / chinese-novelist-skill / kimi-writer → 线性或全自动生成；
  - storyforge / OpenFic / denova / neuro-book / AI-Novel-Writing-Assistant → 需要人工手动全程定向，操作门槛高。

本项目把"剧情决策"产品化为**抽卡游玩机制**（低门槛、高娱乐性 + 收集感），同时保留自由输入（高自由度），并复用开源生态最成熟的工程能力。

### 1.3 目标
- G1｜用户能以「抽卡」或「自由输入」两种方式持续、自然地决定剧情走向，且全程不需要任何写作/编程知识。
- G2｜生成正文：剧情连贯一致（角色不吃书、伏笔可回收、时间线不出错）、可读性强、去 AI 味。
- G3｜支持长篇连载（目标 ≥ 50 章 / 单章 2000–3000 字）。
- G4｜能复用主要开源模型（OpenAI 兼容）与本地模型，支持 BYOK。

### 1.4 非目标（Out of Scope）
- 不做网文平台扫榜/签约/运营、不上架收费。
- 不做漫画/短剧等跨媒介自动衍生（预留扩展点）。
- 不做多人在线协作编辑（V1 单人单项目）。
- 不做针对某个平台文风的强合规过滤，仅做通用"去 AI 味"。

---

## 2. 用户画像
| 画像 | 描述 | 核心诉求 |
|------|------|----------|
| 互动小说 / 文字游戏玩家 | 喜欢"选择驱动剧情"，玩过跑团/视觉小说 | 低门槛、有意外与收集、剧情不崩 |
| 抽卡 / 收集爱好者 | 享受开盲盒、集卡 | 卡牌有稀有度、有图鉴收集 |
| 网文读者 / 轻量写手 | 想低成本获得定向故事 | 自由输入、文风可控、能长篇连载 |

---

## 3. 核心玩法设计（需求核心）

每次剧情推进到「分歧点」时，系统暂停生成并给出决策界面，用户二选一：

### 3.1 抽卡（Gacha）
系统（由 LLM 结合当前故事状态）当场生成 **3–5 张「命运卡」**，每张卡代表一个可能的剧情方向。支持两种抽取模式：

- **盲抽（Draw）**：玩家不预先看卡面，点击后随机揭晓一张。强调随机戏剧性与惊喜。
- **明选（Pick）**：同时展示各张卡的卡面文案 + 「后果标签」（如：机遇 / 危机 / 角色 / 转折），玩家选一张。

卡牌具备**稀有度**（N / R / SR / SSR）：
- 稀有度由卡的方向张力、风险-回报、对主线影响程度决定；
- SSR 卡通常带更强戏剧冲突或更大分叉（例如改变主线走向、引入隐藏设定）；
- 稀有度同时影响抽中的基础权重与演出效果（更强的卡片文案/场景描述）。

卡牌典型类型（由方向事件生成器产出）：
- **事件卡**：随机推动剧情的事件（冲突、意外、机遇）。
- **行动卡**：给主角的选项（角色行动分支）。
- **场景卡**：场景/环境转移。
- **相遇卡**：引入新角色 / 关系变化。
- **伏笔卡**：埋下/触发伏笔，与一致性模块联动。

### 3.2 自由输入（Free Input）
玩家随时可输入任意文本指令，系统：
- 用「意图理解」把自然语言转为剧情命令（事件、对白、设定新增/修改、场景切换、角色登场）；
- 玩家也可对抽到的某张卡写"自定义改写"后进行采用（混合玩法）；
- 输入被采纳后，与正文生成器 / 状态更新模块联动，保证剧情仍自洽。

### 3.3 决策兜底原则
无论抽到哪张卡、还是自由输入，生成器都必须让叙事**自然接续**当前状态（由方向采纳器 + 一致性校验兜底），不允许"断线感"或明显跳戏。

---

## 4. 功能需求

**优先级定义**：P0 = 首版必须；P1 = 重要，二版；P2 = 可选增强。

### FR-1 故事创建与初始化　[P0]
- 描述：用户输入一句话灵感/题材（或随机生成），系统产出本项目的基础：书名/标题候选、世界观设定、初始角色（主角+配角）、初始主线方向、初始抽卡池。
- 验收：给出 3 套整本方向候选；每套含世界观+角色卡+主线一句话；用户可选或要求重roll。

### FR-2 正文流式生成　[P0]
- 描述：基于当前故事状态与已采用的方向，流式输出下一段正文（逐 token 显示），并持续追加到当前章节。
- 验收：流式 TTFB ≤ 3s（网络正常时）；输出可随时暂停/继续；生成内容自动写入项目正文库。

### FR-3 剧情分歧点检测与生成　[P0]
- 描述：生成器在正文到达"可决策节点"时，自动判定进入分歧；系统生成候选方向（卡池）。
- 验收：每章自然出现 1–3 个分歧点；候选方向与当前剧情强相关（不跑题）。

### FR-4 抽卡系统　[P0 · 核心]
- 描述：见 §3.1。生成 3–5 张卡，支持盲抽/明选、稀有度、后果标签、卡面文案渲染。
- 验收：抽卡结果为随机且可配置权重；选定后能正确触发对应方向；稀有度分级正确；卡面无乱码/超长。

### FR-5 自由输入引导　[P0 · 核心]
- 描述：见 §3.2。自由输入、对卡的改写、意图理解。
- 验收：常见意图（加角色/加事件/加设定/切场景/抛伏笔）能被理解并生效；明显跑题的输入有提示但允许；改写后采用不破坏一致性。

### FR-6 一致性 / 状态维护　[P0]
- 描述：维护「世界状态 + 角色状态 + 时间线 + 伏笔账本」，生成前后读写，避免吃书、伏笔可回收。
- 验收：抽取 20 个角色属性/设定项，跨 30 章抽查无不一致；已埋伏笔在用户触发时能正确回收/兑现；时间线不出现前后矛盾。

### FR-7 文风与去 AI 味　[P1]
- 描述：提供文风预设（可选），并对生成稿做"去 AI 味"规则 lint（消除模板句、机械过渡、公式化表达），给阻断/建议。
- 验收：示例稿件通过去 AI 味规则扫描，无命中 0 分档违禁项；提供至少 3 种文风预设。

### FR-8 记忆与长时间上下文　[P1]
- 描述：多级上下文 + 自动压缩 + 章节摘要 + 关键事实检索（RAG 可选），支撑长篇。
- 验收：单章节正文不使用超过模型窗口 80%；长文中早期关键设定/伏笔能在后续被召回；token 消耗上榜可见。

### FR-9 存档 / 分支 / 回溯　[P0]
- 描述：自动存档；支持从任意历史决策点"重走"（在选定的分歧点重新抽卡/重新输入，产生替代剧情分支）；可回退单步。
- 验收：可回溯到任意已发生决策点并产生新分支；回退/分支不影响其他项目；分支可命名。

### FR-10 多模型接入（BYOK）与成本控制　[P1]
- 描述：模型层统一走 OpenAI 兼容协议，支持多服务商 + 本地模型（Ollama 等）；按任务路由；token 预算保护与用量统计。
- 验收：可配置 ≥3 家服务商 + 本地模型；不同任务用不同模型/温度；达预算自动告警/暂停。

### FR-11 用户界面　[P0]
- 描述：Web 界面为主（可后续加桌面/移动/PWA）。包含：项目列表、故事读写页（正文 + 流式）、**决策页（抽卡动画/明选/输入框）**、设定/角色/伏笔视图、图鉴收集、历史/分支视图。
- 验收：决策流程 ≤3 次点击可完成一次抽卡；正文可阅读/滚动/复制；全流程无刷新。

### FR-12 自重审稿 / 质量反馈　[P1]
- 描述：生成后可进入"审稿-修复"：系统产出结构化审稿报告（连贯性/设定吻合/文笔），可一键提交修复。
- 验收：审稿报告列出问题与对应正文证据；一键修复能替换相应文本并保留其余。

### （P2 可选）
- 卡牌图鉴收集与稀有度收藏进度。
- 角色属性影响抽卡倾向/权重。
- 结局统计 / 分支树可视化。
- 多人在线房间、公开分享。

---

## 5. 非功能需求（NFR）

| 类别 | 需求 |
|------|------|
| 性能 | 流式 TTFB ≤ 3s；单请求生成不卡死界面；并发 10 用户不显著降级 |
| 可用性 | 生成中途可暂停/恢复；进程中断可断点续(S3 存档) |
| 可扩展 | 模型接入插件化；任务可按需加 Agent；可加新文风/卡类型 |
| 数据安全 | 本地优先，项目数据默认留在用户端；云端模式可选项；不收集正文用于训练 |
| 成本 | 提供 token 用量看板；自动压缩与 L2 按需拉取抑制成本 |
| 兼容 | 前端主流浏览器（Chrome/Edge/Firefox）；可选 PWA / Capacitor（Vue 生态移动端） |
| 可维护 | 后端接口文档化（OpenAPI）；日志与可观测 |

---

## 6. 优先级矩阵

| 需求 | 优先级 | 一句话理由 |
|------|--------|-----------|
| FR-1 初始化 | P0 | 起步必需 |
| FR-2 正文生成 | P0 | 核心产出 |
| FR-3 分歧点检测 | P0 | 决策入口 |
| FR-4 抽卡系统 | P0 | 核心差异化 |
| FR-5 自由输入 | P0 | 核心差异化 |
| FR-6 一致性 | P0 | 不崩盘底线 |
| FR-9 存档/回溯 | P0 | 玩法闭环 |
| FR-11 UI（决策页） | P0 | 体验载体 |
| FR-7 去 AI 味 | P1 | 质量提升 |
| FR-8 长上下文 | P1 | 长篇支撑 |
| FR-10 多模型 | P1 | 灵活与省钱 |
| FR-12 审稿 | P1 | 质量闭环 |
| P2 各项 | P2 | 锦上添花 |

---

## 7. 成功指标（可量化）
- 首次会话生成完整故事的概率 / 完成率。
- 平均每个决策点从出现到完成的交互时长（目标 < 30s）。
- 抽卡后剧情"自然接续"的用户主观满意度（抽样问卷 ≥ 4/5）。
- 连续 ≥ 20 章的一致性错误数（目标 ≤ 2 处重大矛盾）。
- 去 AI 味规则扫描通过率。